appId: wk.example # 应用ID
artifactName: "${productName}-${version}.${ext}" # 应用最终的名称
productName: 'electronTest' # 打包完成后的应用名称
npmRebuild: false # 是否在开始编译前重构原生依赖,可先尝试true, 如果出现问题再修改为false,

electronVersion: 9.0.5

#directories:
  #output: electron_packed/